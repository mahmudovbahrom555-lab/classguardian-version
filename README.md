# classguardian-version

Just a version-check endpoint for the ClassGuardian Agent — read by
`UpdateChecker` in the agent to notify a teacher when a newer build exists.
No source code lives here; the actual project is in a private repo.
